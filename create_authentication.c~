#include <stdio.h>

#include <types.h>
#include <util.h>


bool writeAuth(FILE * fd, char * username, char * password) {
	bool ret = FALSE;

	CHECK(fd != NULL);
	CHECK(fd != NULL);
	CHECK(fd != NULL);

LBL_ERROR:
	ret = FALSE;

LBL_CLEANUP:
	return ret;
}


int main(int argc, char ** argv)
{
	

}
